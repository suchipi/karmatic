it("should use the stage-0 preset out of the box", () => {
  // should not fail to parse
  function newSyntax() {
    /* eslint-disable */
    foo::bar();
    const { abc } = { ...oneTwoThree };
    return new.target;
  }
});
